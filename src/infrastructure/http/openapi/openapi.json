{
  "openapi": "3.0.3",
  "info": {
    "title": "Arrow Maze API",
    "version": "0.3.0",
    "description": "API REST para Arrow Maze: autenticación JWT, catálogo de niveles con seed, progreso y leaderboard."
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT",
        "description": "Token emitido por POST /auth/login"
      }
    }
  },
  "paths": {
    "/health": {
      "get": {
        "summary": "Chequeo de disponibilidad del servidor",
        "responses": {
          "200": {
            "description": "El servidor está operativo",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "status": { "type": "string", "example": "ok" }
                  }
                }
              }
            }
          }
        }
      }
    },
    "/auth/register": {
      "post": {
        "summary": "Registra un nuevo usuario",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["username", "password"],
                "properties": {
                  "username": { "type": "string", "minLength": 3, "maxLength": 20 },
                  "password": { "type": "string", "minLength": 8 }
                }
              }
            }
          }
        },
        "responses": {
          "201": { "description": "Usuario creado" },
          "400": { "description": "Body inválido" },
          "409": { "description": "El username ya existe" }
        }
      }
    },
    "/auth/login": {
      "post": {
        "summary": "Inicia sesión y devuelve un token JWT",
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["username", "password"],
                "properties": {
                  "username": { "type": "string" },
                  "password": { "type": "string" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Login exitoso, devuelve token" },
          "400": { "description": "Body inválido" },
          "401": { "description": "Credenciales inválidas" }
        }
      }
    },
    "/progress/sync": {
      "post": {
        "summary": "Sincroniza el progreso de un jugador en un nivel",
        "security": [{ "bearerAuth": [] }],
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "type": "object",
                "required": ["userId", "levelId", "score", "moves", "timeInSeconds", "completed"],
                "properties": {
                  "userId": { "type": "string" },
                  "levelId": { "type": "string" },
                  "score": { "type": "integer" },
                  "moves": { "type": "integer" },
                  "timeInSeconds": { "type": "integer" },
                  "completed": { "type": "boolean" }
                }
              }
            }
          }
        },
        "responses": {
          "200": { "description": "Progreso sincronizado" },
          "400": { "description": "Body inválido" },
          "401": { "description": "JWT ausente o inválido" },
          "404": { "description": "El nivel no existe" }
        }
      }
    },
    "/progress": {
      "get": {
        "summary": "Obtiene todo el progreso del usuario autenticado",
        "description": "Devuelve el progreso de todos los niveles del usuario identificado por el JWT, para que el cliente lo descargue y fusione al iniciar sesión desde un dispositivo nuevo.",
        "security": [{ "bearerAuth": [] }],
        "responses": {
          "200": {
            "description": "Progreso del jugador",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "properties": {
                    "userId": { "type": "string" },
                    "levels": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "userId": { "type": "string" },
                          "levelId": { "type": "string" },
                          "highScore": { "type": "integer" },
                          "minMoves": { "type": "integer" },
                          "minTimeInSeconds": { "type": "integer" },
                          "isCompleted": { "type": "boolean" }
                        }
                      }
                    }
                  }
                }
              }
            }
          },
          "401": { "description": "JWT ausente o inválido" }
        }
      }
    },
    "/leaderboard/{levelId}": {
      "get": {
        "summary": "Top de clasificación de un nivel",
        "parameters": [
          { "name": "levelId", "in": "path", "required": true, "schema": { "type": "string" } },
          { "name": "limit", "in": "query", "required": false, "schema": { "type": "integer" } }
        ],
        "responses": {
          "200": { "description": "Lista de entradas del leaderboard" }
        }
      }
    },
    "/levels": {
      "get": {
        "summary": "Lista todos los niveles disponibles",
        "responses": {
          "200": { "description": "Lista de niveles" }
        }
      }
    },
    "/levels/{id}": {
      "get": {
        "summary": "Obtiene un nivel por id",
        "parameters": [{ "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }],
        "responses": {
          "200": { "description": "Definición del nivel" },
          "404": { "description": "El nivel no existe" }
        }
      },
      "put": {
        "summary": "Crea o actualiza la definición de un nivel",
        "security": [{ "bearerAuth": [] }],
        "parameters": [{ "name": "id", "in": "path", "required": true, "schema": { "type": "string" } }],
        "responses": {
          "200": { "description": "Nivel guardado" },
          "401": { "description": "JWT ausente o inválido" },
          "422": { "description": "El nivel no es resoluble" }
        }
      }
    }
  }
}
